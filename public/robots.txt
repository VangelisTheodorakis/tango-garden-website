User-agent: *
Allow: /

# Note: the products and legal pages we do not want indexed carry a `noindex`
# meta tag instead of a Disallow rule here. Disallowing them would stop crawlers
# fetching the page at all, so they could never read the noindex — the usual way
# a URL ends up indexed with no title or snippet.

Sitemap: https://tangogarden.de/sitemap-index.xml
